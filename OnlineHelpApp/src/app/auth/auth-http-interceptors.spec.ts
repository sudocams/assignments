import { AuthHttpInterceptors } from './auth-http-interceptors';

describe('AuthHttpInterceptors', () => {
  it('should create an instance', () => {
    expect(new AuthHttpInterceptors()).toBeTruthy();
  });
});
